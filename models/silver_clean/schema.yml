version: 2

models:
  - name: silver_clean_biosignals
    columns:
      - name: participation_id
        data_tests:
          - not_null
          - relationships:
              to: ref('silver_clean_participation')
              field: participation_id
        
      - name: timestamp
        data_tests:
          - not_null

  - name: silver_clean_blendshapes
    columns:
      - name: participation_id
        data_tests:
          - not_null
        
      - name: timestamp
        data_tests:
          - not_null

  - name: silver_clean_emotions
    columns:
      - name: participation_id
        data_tests:
          - not_null
      
      - name: dominant
        data_tests:
          - not_null
          - accepted_values:
              values: ['HAPPY', 'SAD', 'ANGRY', 'SURPRISE', 'DISGUST', 'NEUTRAL']

  - name: silver_clean_participant
    columns:
      - name: participant_id
        data_tests:
          - not_null
          - unique
      
      - name: gender
        data_tests:
          - accepted_values:
              values: ['MALE', 'FEMALE', 'OTHER']

  - name: silver_clean_session
    columns:
      - name: session_id
        data_tests:
          - not_null
          - unique

  - name: silver_clean_participation
    columns:
      - name: participation_id
        data_tests:
          - not_null
          - unique

      - name: participant_id
        data_tests:
          - not_null
          - relationships:
              to: ref('silver_clean_participant')
              field: participant_id

      - name: session_id
        data_tests:
          - not_null
          - relationships:
              to: ref('silver_clean_session')
              field: session_id